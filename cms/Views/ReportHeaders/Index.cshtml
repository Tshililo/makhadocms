
<script src="@Url.Content("~/Scripts/MicrosoftAjax.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/MicrosoftMvcAjax.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/MicrosoftMvcValidation.min.js")" type="text/javascript"></script>

@using (Html.BeginForm())
{



    <script type="text/javascript">

    // Roles Menu Buttons
        function OnReportHeaderMenuItemClick(s, e) {
        //alert("I am here: So ya clicked he button over yonder then: " + e.item.name)
        //https://documentation.devexpress.com/#AspNet/DevExpressWebScriptsASPxClientGridView_UpdateEdittopic
        if (e.item.name == "btnHeaderNew") {

            var index = ReportHeader.GetFocusedRowIndex();
            ReportHeader.AddNewRow(index);
            // call edit funtion from custom button
        }

        if (e.item.name == "btnHeaderEdit") {

            var index = ReportHeader.GetFocusedRowIndex();
           // alert('index : ' + index);

            ReportHeader.StartEditRow(index);
            // call edit funtion from custom button
        }

        if (e.item.name == "btnHeaderDelete") {
            if (confirm("Confirm Delete?")) {
                var index = ReportHeader.GetFocusedRowIndex();
                ReportHeader.DeleteRow(index);
            }
        }

    }

    </script>

    @Html.Partial("ReportHeaderLandingFormView");
}



